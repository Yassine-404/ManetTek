<!DOCTYPE html>
<html>
<head>
    <style>
        body {
            font-family: Arial, sans-serif;
        }

        .header {
            text-align: right;
            margin-bottom: 20px;
        }

        .logo {
            text-align: center;
            margin-bottom: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }

        th, td {
            border: 1px solid #dddddd;
            text-align: left;
            padding: 8px;
        }

        th {
            background-color: #f2f2f2;
        }

        .title {
            text-align: center;
            font-size: 24px;
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
<div class="header">
    Date et heure actuelles : {{ "now"|date('Y-m-d H:i') }}
</div>

<div class="logo">
    <img src="{{ asset('assets/images/logo-2.png') }}" style="width: 100px; height: 100px;" alt="Description de votre image">
</div>

<div class="title">
    <h1>LISTES DES Users</h1>
</div>

<div>
    <table>
        <thead>
        <tr>
            <th>ID</th>
            <th>prenom</th>
            <th>password</th>
            <th>Mail</th>
            <th>Role</th>
            <th>adress</th>
            <th>Ville</th>
            <th>zipcode</th>
        </tr>
        </thead>
        <tbody>
        {% for utilisateur in users %}
            <tr>
                <td>{{ utilisateur.id }}</td>
                <td>{{ utilisateur.prenom }}</td>
                <td>{{ utilisateur.password }}</td>
                <td>{{ utilisateur.email }} </td>
                <td>{{ utilisateur.role }}</td>
                <td>{{ utilisateur.adress }}</td>
                <td>{{ utilisateur.ville }}</td>
                <td>{{ utilisateur.zipcode }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
</body>
</html>
